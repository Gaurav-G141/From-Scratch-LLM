# Training smoke-test dependencies (Day 2 full loop)
# Install after base requirements: pip install -r requirements-train.txt

peft>=0.13.0
datasets>=3.0.0
trl>=0.12.0

# Optional — CUDA only, ~2x faster QLoRA:
# pip install unsloth
